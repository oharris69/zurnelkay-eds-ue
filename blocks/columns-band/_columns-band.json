{
  "definitions": [
    {
      "title": "Columns Band",
      "id": "columns-band",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Columns Band",
              "model": "columns-band",
              "filter": "columns-band"
            }
          }
        }
      }
    },
    {
      "title": "Band Cell",
      "id": "columns-band-cell",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block/item",
            "template": {
              "name": "Band Cell",
              "model": "columns-band-cell"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "columns-band",
      "fields": [
        {
          "component": "select",
          "name": "classes",
          "label": "Image Side",
          "value": "",
          "options": [
            { "name": "Image Left (default)", "value": "" },
            { "name": "Image Right", "value": "image-right" }
          ]
        }
      ]
    },
    {
      "id": "columns-band-cell",
      "fields": [
        {
          "component": "richtext",
          "name": "content",
          "value": "",
          "label": "Content",
          "valueType": "string"
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "columns-band",
      "components": [
        "columns-band-cell"
      ]
    }
  ]
}
